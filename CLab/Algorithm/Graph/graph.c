#include <stdio.h>
#include <stdlib.h>
#include "graph.h"

void CreateMGraph(MGraph *G)
{
    int i, j, k, w;
    char ch;
    for (i = 0; i < G->n; i++)
	scanf("%d", &G->vex[i]);
}

int main()
{
    return 0;
}
