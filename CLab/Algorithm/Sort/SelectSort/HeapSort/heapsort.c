#include <stdio.h>
#include <stdlib.h>

#include "heapsort.h"

/**************************************
 k[i] <= {k[2i], k[2i+1]} 
 or
 k[i] >= {k[2i], k[2i+1]}
**************************************/

void Heap_Adjust(datatype *arr, int s, int t)
{
    int i, j, k;
    for (i = 0; i)
}

void Heap_Sort(datatype *data, int n)
{
    
}

int main()
{
    
    return 0;
}
