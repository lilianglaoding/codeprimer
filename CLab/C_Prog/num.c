#include <stdio.h>
#include <stdlib.h>

double getnum()
{
    double a;
    int i = 1;
    int flag[10] = {0};
    for (i = 1; i < 10; i++)
    {
	if (flag[i] != 1)
	{
	    if (a % )
	    a = a * 10;
	}
    }
}
