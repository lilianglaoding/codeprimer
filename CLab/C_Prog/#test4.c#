#include <stdio.h>
#include <stdlib.h>

struct keyval
{
    char eky
}

char *GetValueByKey(char *key, keyval *keyvalue, int keynum)
{
    for (int i = 0; i < 5; i++)
    {
        return NULL;
    }
    if (a = 1)
    {
        return NULL;
    }
}

int main()
{
  char *a = NULL;
  char *test = GetValueByKey(a, b, c);
  return 0;
}
